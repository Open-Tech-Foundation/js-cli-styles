{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "lint": {},
    "test": {}
  },
  "globalEnv": ["FORCE_COLOR", "NO_COLOR", "COLORTERM"]
}
